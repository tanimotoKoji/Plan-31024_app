#account-page.account-page
  .account-page__inner.clearfix
    .account-page__inner--left.account-page__header
      %h2 Create Account
      %h5 新規アカウントの作成
    .account-page__inner--right.user-form
      = form_with model: resource, as: resource_name, url: registration_path(resource_name), local: true do |f|
        = devise_error_messages!
        .field
          .field-label
            = f.label :nickname
          .field-input
            = f.text_field :nickname, autofocus: true
        .field
          .field-label
            = f.label :email
          .field-input
            = f.email_field :email
        .field
          .field-label
            = f.label :password
            %i (英数字6文字以上)
          .field-input
            = f.password_field :password, autocomplete: "off"
        .field
          .field-label
            = f.label :password_confirmation
          .field-input
            = f.password_field :password_confirmation, autocomplete: "off"
        .field
          .field-label
            = f.label :profile
          .field-input
            = f.text_field :profile, autofocus: true
        .field
          .field-label
            = f.label :gender_id
            %label
              = f.radio_button :gender_id, 1
              男性
            %label
              = f.radio_button :gender_id, 2
              女性
            %label
              = f.radio_button :gender_id, 3
              その他
        .field
          .field-label
            = f.label :age_id
            %label
              = f.radio_button :age_id, 1
              10歳〜19歳
            %label
              = f.radio_button :age_id, 2
              20歳〜29歳
            %label
              = f.radio_button :age_id, 3
              30際〜39際
            %label
              = f.radio_button :age_id, 4
              40歳〜49歳
            %label
              = f.radio_button :age_id, 5
              50歳〜59歳
            %label
              = f.radio_button :age_id, 6
              60歳以上
        .field
          .field-label
            = f.label :occupation_id
          .field-input
            = f.collection_select(:occupation_id, Occupation.all, :id, :name, {}, {class:"occupation-select"})
        .form-group
          %p.form-info-header
            本人確認
          %p.form-info-text
            安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
        .form-group
          .form-text-wrap
            %label.form-text お名前(全角)
            %span.indispensable 必須
          .input-name-wrap
            = f.text_area :first_name, class:"input-name", id:"first-name", placeholder:"例) 山田"
            = f.text_area :last_name, class:"input-name", id:"last-name", placeholder:"例) 陸太郎"
        .form-group
          .form-text-wrap
            %label.form-text お名前カナ(全角)
            %span.indispensable 必須
          .input-name-wrap
            = f.text_area :first_name_kana, class:"input-name", id:"first-name-kana", placeholder:"例) ヤマダ"
            = f.text_area :last_name_kana, class:"input-name", id:"last-name-kana", placeholder:"例) リクタロウ"
        .img-upload
          .weight-bold-text
            プロフィール写真
            %span.indispensable 必須
          .click-upload
            %p
              クリックしてファイルをアップロード
            = f.file_field :image, id:"item-image"
        .actions
          = f.submit "Create Account", class: 'btn'
